# Secrets (WiFi credentials, API keys, Home Assistant token)
firmware/agente-ia/secrets.h
server/secrets.local.ps1
server/.env

# Persistent agent memory (personal: the human's name, facts, mood)
server/agent_state.json
server/admin_config.json

# YouTube Music (cookies locales, no commitear)
server/ytmusic_cookies.txt
server/.cache-music/

# Python
server/.venv/
server/.venv-rvc/
server/.cache-suno/
server/.hf-cache/
server/.pip-tmp/
server/.pip-cache/
server/__pycache__/
*.pyc

# Scratch / temp
tmp/
*.log

# Build artifacts (firmware) — regenerables
firmware/agente-ia/build/
firmware/build/
firmware/agente-ia/spiffs.bin

# Copias manuales / snapshots locales (los checkpoints reales son tags git)
checkpoints/
firmware/checkpoints/
firmware/commit/
firmware/agente-iaV1/

# Modelos / audio pesado (no son fuente; se descargan o generan)
server/rvc_models/
server/voices/
server/debug_audio/
server/out_test.wav
server/err.txt

# Documentación interna de checkpoint / migración (solo local, no remoto)
PROJECT_SNAPSHOT.md
RESTORE.md
AURA_MIGRATION_PLAN.md
CHANGELOG.md
VERSION

# IDE / agente / memoria local
.cursor/
.engram/
.atl/
.mcp.json

# Local-only paths
docs/marketing/
docs/agenteia-manual/
docs/linkedin-teaser-espectativa.md
scripts/build-marketing.py
scripts/build-manual-pdf.py
