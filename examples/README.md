# Examples

- `minimal-theme/` — skeleton `theme.json` + layout for a custom HMI
- `custom-theme/` — override colors extending KITT
- `custom-widget/` — `GaugeWidget.h` sample for AURA widgets
- `custom-personality/` — server personality snippet
- `custom-device/` — hardware profile stub
- `hello-plugin/` — server plugin stub (Fase 6)

Start here: `sdk/GETTING_STARTED.md`
