# Custom widget example

Copy `GaugeWidget.h` into `firmware/agente-ia/aura/widgets/` and register it in your theme's `widgets.json`.

See `sdk/contracts/widget_spec.schema.json` for the declarative schema.
